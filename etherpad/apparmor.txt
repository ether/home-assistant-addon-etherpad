#include <tunables/global>

profile etherpad flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>
  #include <abstractions/ssl_certs>

  capability net_bind_service,
  capability setgid,
  capability setuid,
  capability dac_override,

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network unix stream,
  network unix dgram,

  signal,

  # S6-Overlay + bashio
  /init rix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /etc/s6-overlay/** rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /etc/fix-attrs.d/** rwix,
  /run/** rwk,
  /usr/lib/bashio/** ix,
  /tmp/** rw,

  # Etherpad runtime files
  /opt/etherpad-lite/** r,
  /opt/etherpad-lite/var/** rw,
  /opt/etherpad-lite/node_modules/** rwk,
  /opt/etherpad-lite/src/node/** ix,
  /usr/local/lib/node_modules/** rix,

  # HA add-on data + ssl mount
  /data/** rw,
  /ssl/** r,
  /etc/etherpad/** rw,

  # Proc + sys read for node introspection
  /proc/sys/** r,
  /proc/[0-9]*/** r,
  /sys/devices/system/cpu/** r,
}
